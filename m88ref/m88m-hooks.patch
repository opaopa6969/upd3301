diff --git a/src/devices/Z80c.cpp b/src/devices/Z80c.cpp
index 86c43a4..a237c84 100644
--- a/src/devices/Z80c.cpp
+++ b/src/devices/Z80c.cpp
@@ -468,14 +468,19 @@ int Z80C::cbase;
 // ---------------------------------------------------------------------------
 //	1 ���ߎ��s
 //	
+extern void* g_mainCpu;
+extern void (*g_pcHook)(unsigned pc);
 inline void Z80C::SingleStep()
 {
+	if (g_pcHook && (void*)this == g_mainCpu) g_pcHook(GetPC());
 	SingleStep(Fetch8());
 }
 
 // ---------------------------------------------------------------------------
 //	I/O �����̒�` -----------------------------------------------------------
 
+extern void (*g_rdHook)(unsigned pc, unsigned addr, unsigned val);
+extern unsigned g_rdLo, g_rdHi;
 inline uint Z80C::Read8(uint addr)
 {
 	addr &= 0xffff;
@@ -487,18 +492,27 @@ inline uint Z80C::Read8(uint addr)
 #endif
 	{
 		DEBUGCOUNT(12);
-		return ((uint8*)page.ptr)[addr & pagemask];
+		{ uint v_ = ((uint8*)page.ptr)[addr & pagemask];
+		  if (g_rdHook && (void*)this == g_mainCpu && addr >= g_rdLo && addr <= g_rdHi) g_rdHook(GetPC(), addr, v_);
+		  return v_; }
 	}
 	else
 	{
 		DEBUGCOUNT(8);
-		return (*MemoryManager::RdFunc(intpointer(page.ptr) & ~idbit))(page.inst, addr);
+		{ uint v_ = (*MemoryManager::RdFunc(intpointer(page.ptr) & ~idbit))(page.inst, addr);
+		  if (g_rdHook && (void*)this == g_mainCpu && addr >= g_rdLo && addr <= g_rdHi) g_rdHook(GetPC(), addr, v_);
+		  return v_; }
 	}
 }
 
+extern void (*g_e6cdHook)(unsigned pc, unsigned addr, unsigned val);
+extern void (*g_wrHook)(unsigned pc, unsigned addr, unsigned val);
+extern unsigned g_wrLo, g_wrHi;
 inline void Z80C::Write8(uint addr, uint data)
 {
 	addr &= 0xffff;
+	if (g_e6cdHook && (addr == 0xe6cd || addr == 0xec88 || addr == 0xec89)) g_e6cdHook(GetPC(), addr, data);
+	if (g_wrHook && (void*)this == g_mainCpu && addr >= g_wrLo && addr <= g_wrHi) g_wrHook(GetPC(), addr, data);
 	MemoryPage& page = wrpages[addr >> pagebits];
 #ifdef PTR_IDBIT
 	if (!(intpointer(page.ptr) & idbit))
@@ -529,8 +543,12 @@ inline uint Z80C::Read16(uint addr)
 	{
 		DEBUGCOUNT(13);
 		uint a = addr & pagemask;
-		if (a < pagemask)
-			return *(uint16*)((uint8*)page.ptr + a);
+		if (a < pagemask) {
+			uint v_ = *(uint16*)((uint8*)page.ptr + a);
+			if (g_rdHook && (void*)this == g_mainCpu && addr >= g_rdLo && addr <= g_rdHi)
+				{ g_rdHook(GetPC(), addr, v_ & 0xff); g_rdHook(GetPC(), addr + 1, (v_ >> 8) & 0xff); }
+			return v_;
+		}
 	}
 #endif
 	return Read8(addr) + Read8(addr+1) * 256;
diff --git a/src/pc88/base.cpp b/src/pc88/base.cpp
index ccd0dc2..2e1bd11 100644
--- a/src/pc88/base.cpp
+++ b/src/pc88/base.cpp
@@ -107,8 +107,10 @@ void IOCALL Base::RTC(uint)
 // ---------------------------------------------------------------------------
 //	Vertical Retrace Interrupt
 //
+extern void (*g_vrtcHook)(unsigned en);
 void IOCALL Base::VRTC(uint, uint en)
 {
+	if (g_vrtcHook) g_vrtcHook(en);
 	if (en)
 	{
 		pc->VSync();
diff --git a/src/pc88/fdc.cpp b/src/pc88/fdc.cpp
index e288b42..7cd1c44 100644
--- a/src/pc88/fdc.cpp
+++ b/src/pc88/fdc.cpp
@@ -20,6 +20,7 @@
 #include "diag.h"
 
 using namespace PC8801;
+extern unsigned g_fdcDataCount; extern void (*g_fdcResultHook)(unsigned,unsigned,unsigned,unsigned,unsigned,unsigned,unsigned);
 
 // ---------------------------------------------------------------------------
 //	�\�z/����
@@ -265,8 +266,7 @@ uint IOCALL FDC::GetData(uint)
 
 		// E-Phase(�]����)
 		case execreadphase:
-//			LOG1("ex= %d\n", scheduler->GetTime());
-//			LOG0("*");
+			g_fdcDataCount++;
 			data = *bufptr++;
 			if (--count)
 			{
@@ -409,6 +409,7 @@ void FDC::ShiftToResultPhase7()
 	buffer[4] = idr.h;
 	buffer[5] = idr.r;
 	buffer[6] = idr.n;
+	if (::g_fdcResultHook) ::g_fdcResultHook(buffer[0],buffer[1],buffer[2],buffer[3],buffer[4],buffer[5],buffer[6]);
 	Intr(true);
 	ShiftToResultPhase(7);
 }
@@ -608,8 +609,10 @@ void FDC::CmdScanEqual()
 
 
 
+extern void (*g_fdcReadHook)(unsigned c, unsigned h, unsigned r, unsigned n, unsigned eot);
 void FDC::ReadData(bool deleted, bool scan)
 {
+	if (g_fdcReadHook) g_fdcReadHook(idr.c, idr.h, idr.r, idr.n, eot);
 	LOG4("\tRead %.2x %.2x %.2x %.2x\n", idr.c, idr.h, idr.r, idr.n);
 	if (showstatus)
 		statusdisplay.Show(85, 0, "%s (%d) %.2x %.2x %.2x %.2x", scan ? "Scan" : "Read", hdu & 3, idr.c, idr.h, idr.r, idr.n);
diff --git a/src/pc88/subsys.cpp b/src/pc88/subsys.cpp
index 934f9e1..ea90c09 100644
--- a/src/pc88/subsys.cpp
+++ b/src/pc88/subsys.cpp
@@ -16,6 +16,7 @@
 #include "diag.h"
 
 using namespace PC8801;
+extern void (*g_mrdHook)(unsigned port, unsigned val);
 
 // ---------------------------------------------------------------------------
 //	�\�z�E�j��
@@ -177,7 +178,7 @@ uint IOCALL SubSystem::M_Read0(uint)
 {
 	idlecount = 0;
 	uint d = piom.Read0();
-	LOG1(">%.2x ", d);
+	if (::g_mrdHook) ::g_mrdHook(0, d);
 	return d;
 }
 
